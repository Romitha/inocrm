- if params[:status_param] == "initiate" or (@new_accessory and @new_accessory.errors.present?)
  $("#form_frame").html("#{j render 'tickets/accessory'}");
- elsif params[:status_param] == "create" or params[:status_param] == "back"
  $("#product_load").html("#{j render 'tickets/form', product: @product, ticket: @ticket}");

Tickets.autosave_form("new_ticket", "saveTicket");
Organizations.enable_chosen();
