%table.table
	%thead
		%tr
			%th hours
			%th Description
			%th Function
	%tbody
		- @slas.each do |sla|
			%tr
				%td= sla.sla_time
				%td= sla.description
				%td= link_to "select", "#", onclick: "Tickets.select_sla('#{sla.id}'); false;"