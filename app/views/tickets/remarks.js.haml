$(".refresh").remove();
$("body").removeClass("modal-open");
$(".modal-backdrop").remove();
$("#warranty_details").html("#{j render 'tickets/remarks_form', ticket: @ticket}");

Tickets.chosen_select();

Tickets.first_resolution_visible();